Code for Gas thermODynamics and Matter distribution using jAX (GODMAX). This is also one of the first implementation of halo model in JAX. It also includes a fully JAX-version of mcfit () such that the gradients flow through it and it can be used for gradient-based sampling. Ready to run on GPUs and with differentiable likelihood, and numpyro-integration for HMC sampling. 